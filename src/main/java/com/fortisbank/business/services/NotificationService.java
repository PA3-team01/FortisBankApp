package com.fortisbank.business.services;
/**
 * NotificationService is responsible for sending system alerts and messages
 * to both clients and bank managers.
 *
 * Responsibilities:
 * - Sends real-time notifications when:
 *   • A transaction occurs (deposit, withdrawal, transfer).
 *   • A client’s balance drops below a critical threshold.
 *   • A client requests a new account.
 * - Triggers alerts to bank managers when action is required.
 * - Designed to support multiple notification channels (e.g., UI popups, logs, emails).
 *
 * This service ensures user awareness and regulatory compliance for critical events.
 */

public class NotificationService {
}
